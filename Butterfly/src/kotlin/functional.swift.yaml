---
- id: kotlin.let
  type: call
  template:
    pattern: "(~0~)(~this~)"
    imports: [Butterfly]

- id: kotlin.run
  type: call
  template:
    pattern: "(~0~)(~this~)"
    imports: [Butterfly]

- id: kotlin.with
  type: call
  template:
    pattern: "(~1~)(~0~)"
    imports: [Butterfly]

- id: kotlin.also
  type: call
  template:
    pattern: "also(~this~, ~0~)"
    imports: [Butterfly]

- id: kotlin.apply
  type: call
  template:
    pattern: "also(~this~, ~0~)"
    imports: [Butterfly]

- id: kotlin.takeIf
  type: call
  template:
    pattern: takeIf(~this~, ~0~)
    imports: [Butterfly]

- id: kotlin.takeUnless
  type: call
  template:
    pattern: takeUnless(~this~, ~0~)
    imports: [Butterfly]